<Window x:Class="PDV.WPF.Views.QuantityDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:conv="clr-namespace:PDV.WPF.Converters"
        Title="Adicionar Produto"
        Height="260" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Style="{StaticResource MaterialDesignWindow}"
        Loaded="Window_Loaded"
        KeyDown="Window_KeyDown">
    <Window.Resources>
        <conv:DecimalInputConverter            x:Key="DecimalInput"/>
        <conv:EmptyStringToVisibilityConverter x:Key="StrToVis"/>
    </Window.Resources>

    <StackPanel Margin="28,24,28,20">

        <!-- Produto -->
        <TextBlock Text="{Binding ProductName}"
                   FontSize="18" FontWeight="Bold"
                   TextTrimming="CharacterEllipsis"
                   Margin="0,0,0,4"/>

        <!-- Preço + unidade -->
        <TextBlock Opacity="0.65" Margin="0,0,0,20">
            <Run Text="Preço: "/>
            <Run Text="{Binding SalePrice, StringFormat=C2, Mode=OneWay}"
                 FontWeight="SemiBold"
                 Foreground="{DynamicResource MaterialDesign.Brush.Primary}"/>
            <Run Text="   Unidade: "/>
            <Run Text="{Binding Unit, Mode=OneWay}"/>
        </TextBlock>

        <!-- Quantidade -->
        <TextBox x:Name="TxtQty"
                 materialDesign:HintAssist.Hint="Quantidade"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 FontSize="20" TextAlignment="Center"
                 Text="{Binding Quantity,
                        Converter={StaticResource DecimalInput},
                        UpdateSourceTrigger=LostFocus}"
                 Margin="0,0,0,6"/>

        <!-- Erro -->
        <TextBlock Text="{Binding ErrorMessage}"
                   Foreground="#C62828" FontSize="11"
                   Margin="0,0,0,10"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StrToVis}}"/>

        <!-- Botões -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancelar"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Command="{Binding CancelCommand}"
                    Width="100" Margin="0,0,10,0"/>
            <Button Content="✓  Adicionar"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding ConfirmCommand}"
                    IsDefault="True" Width="120"/>
        </StackPanel>

    </StackPanel>
</Window>
